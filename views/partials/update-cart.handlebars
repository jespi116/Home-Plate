<div class="row row-cols-1 row-cols-md-1 row-cols-sm-1 border-0 w-100 h-100">
    <div class="card">
        <div class="p-3 m-3 d-flex justify-content-between align-items-center add-form text-dark">
           {{!--  <form class="update"> --}}
                <span>&nbsp;</span>
                <input id="quant" type="number" class="quant" value="{{quantity}}" onclick="updateCartHandler({{id}}, {{user_id}}, {{product_id}}, this.value)">
                <span>&nbsp;</span>
                {{!-- <button class=" quantity btn btn-primary" attr1="{{product_id}}" attr2="{{user_id}}" attr3="{{id}}"  onclick="updateCartHandler({{id}}, {{user_id}}, {{product_id}}, {{quantity}}) ">Qyt</button> --}}
                <span>&nbsp;</span>
                <button attr="{{id}}" class="remove btn btn-primary" onclick="deleteCartHandler({{id}})">Remove</button>
           {{!--  </form> --}}
        </div> 
    </div>
</div>